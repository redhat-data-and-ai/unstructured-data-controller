# This rule is not used by the project unstructured-data-controller itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants read-only access to operator.dataverse.redhat.com resources.
# This role is intended for users who need visibility into these resources
# without permissions to modify them. It is ideal for monitoring purposes and limited-access viewing.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: unstructured-data-controller
    app.kubernetes.io/managed-by: kustomize
  name: vectorembeddingsgenerationjob-viewer-role
rules:
- apiGroups:
  - operator.dataverse.redhat.com
  resources:
  - vectorembeddingsgenerationjobs
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - operator.dataverse.redhat.com
  resources:
  - vectorembeddingsgenerationjobs/status
  verbs:
  - get
