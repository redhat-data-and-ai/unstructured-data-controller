apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: datastore-cache
spec:
  accessModes:
    - ReadWriteOncePod
  storageClassName: standard
  resources:
    requests:
      storage: ${PVC_SIZE}
