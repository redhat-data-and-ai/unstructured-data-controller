apiVersion: v1
kind: Service
metadata:
  name: ollama-embedding
  labels:
    app: ollama-embedding
spec:
  ports:
    - name: http
      port: 11434
      targetPort: 11434
  selector:
    app: ollama-embedding
